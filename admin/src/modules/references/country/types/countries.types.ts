// export type TransactionDirection = 'in' | 'out';

// export type TransactionStatus = string;

export type Country = {
  code: string;
  name: string;
  is_blocked: boolean;
  created_at: number;
  updated_at: number;
};
